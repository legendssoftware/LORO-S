{{#base headerTitle="🎯 New Target Set" headerSubtitle="A new target has been assigned to {{userName}}"}}

<div class="greeting">
    Hello {{userName}}! 🎯
</div>

<div class="message">
    <p>A new target has been set for you at {{organizationName}}. This target is designed to help you track your progress and achieve your professional goals.</p>
</div>

<div class="success-notice">
    <p>✅ <strong>New Target Assigned!</strong> Your target has been successfully configured and is now active.</p>
</div>

<div class="info-card">
    <h3>🎯 Target Details</h3>
    <div class="detail-grid">
        <div class="detail-item">
            <span class="detail-label">Employee</span>
            <span class="detail-value">{{userName}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Email</span>
            <span class="detail-value">{{userEmail}}</span>
        </div>
        {{#if targetDetails.dailyHours}}
        <div class="detail-item">
            <span class="detail-label">Daily Hours Target</span>
            <span class="detail-value">{{targetDetails.dailyHours}} hours</span>
        </div>
        {{/if}}
        {{#if targetDetails.monthlyHours}}
        <div class="detail-item">
            <span class="detail-label">Monthly Hours Target</span>
            <span class="detail-value">{{targetDetails.monthlyHours}} hours</span>
        </div>
        {{/if}}
        {{#if targetDetails.description}}
        <div class="detail-item">
            <span class="detail-label">Description</span>
            <span class="detail-value">{{targetDetails.description}}</span>
        </div>
        {{/if}}
        <div class="detail-item">
            <span class="detail-label">Date Set</span>
            <span class="detail-value">{{formatDate createdAt}}</span>
        </div>
    </div>
</div>

<div class="cta-section">
    <a href="{{dashboardUrl}}/targets" class="cta-button">View All Targets</a>
</div>

{{#if mobileAppInfo}}
<div class="info-card" style="background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 100%); border-left-color: #22c55e;">
    <h4>📱 Track on Mobile App</h4>
    <p style="margin: 0; color: #166534; font-size: 14px;">
        <strong>📲 Mobile Access:</strong> Your new targets are now available in our {{mobileAppInfo.appName}}. Download the app to track progress, receive real-time notifications, and stay updated on your performance goals.
    </p>
    <div style="margin-top: 12px; padding: 8px; background: rgba(255,255,255,0.8); border-radius: 6px;">
        <p style="margin: 0; font-size: 13px; color: #166534;">
            🎯 <strong>Mobile Features:</strong>
            {{#each mobileAppInfo.features}}
            • {{this}}<br>
            {{/each}}
        </p>
    </div>
    <div style="margin-top: 12px; text-align: center;">
        <a href="{{mobileAppInfo.appStoreUrl}}" style="background: #22c55e; color: white; padding: 8px 16px; text-decoration: none; border-radius: 20px; font-size: 12px; margin-right: 8px;">📱 iOS App</a>
        <a href="{{mobileAppInfo.googlePlayUrl}}" style="background: #22c55e; color: white; padding: 8px 16px; text-decoration: none; border-radius: 20px; font-size: 12px;">🤖 Android App</a>
    </div>
</div>
{{/if}}

<div class="message">
    <p>You can track your progress and view all your targets in the dashboard or mobile app. If you have any questions about your targets, please contact your manager or our support team.</p>
</div>

{{/base}} 