{{#base headerTitle="📊 Target Updated" headerSubtitle="Your target has been modified - {{userName}}"}}

<div class="greeting">
    Hello {{userName}}! 📊
</div>

<div class="message">
    <p>Your target at {{organizationName}} has been updated. The changes are now active and will be reflected in your performance tracking.</p>
</div>

<div class="info-notice">
    <p>📝 <strong>Target Updated!</strong> Your target configuration has been modified with the latest requirements.</p>
</div>

<div class="info-card">
    <h3>📊 Updated Target Details</h3>
    <div class="detail-grid">
        <div class="detail-item">
            <span class="detail-label">Employee</span>
            <span class="detail-value">{{userName}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Email</span>
            <span class="detail-value">{{userEmail}}</span>
        </div>
        {{#if targetDetails.dailyHours}}
        <div class="detail-item">
            <span class="detail-label">Daily Hours Target</span>
            <span class="detail-value">{{targetDetails.dailyHours}} hours</span>
        </div>
        {{/if}}
        {{#if targetDetails.monthlyHours}}
        <div class="detail-item">
            <span class="detail-label">Monthly Hours Target</span>
            <span class="detail-value">{{targetDetails.monthlyHours}} hours</span>
        </div>
        {{/if}}
        {{#if targetDetails.description}}
        <div class="detail-item">
            <span class="detail-label">Description</span>
            <span class="detail-value">{{targetDetails.description}}</span>
        </div>
        {{/if}}
        <div class="detail-item">
            <span class="detail-label">Last Updated</span>
            <span class="detail-value">{{formatDate updatedAt}}</span>
        </div>
    </div>
</div>

<div class="cta-section">
    <a href="{{dashboardUrl}}/targets" class="cta-button">View All Targets</a>
</div>

{{#if mobileAppInfo}}
<div class="info-card" style="background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border-left-color: #f59e0b;">
    <h4>📱 Updated Targets Available</h4>
    <p style="margin: 0; color: #92400e; font-size: 14px;">
        <strong>🔄 Changes Synced:</strong> Your target updates have been automatically synced to our {{mobileAppInfo.appName}}. View your revised targets and track your progress on-the-go.
    </p>
    <div style="margin-top: 12px; padding: 8px; background: rgba(255,255,255,0.8); border-radius: 6px;">
        <p style="margin: 0; font-size: 13px; color: #92400e;">
            📱 <strong>Mobile Features:</strong>
            {{#each mobileAppInfo.features}}
            • {{this}}<br>
            {{/each}}
        </p>
    </div>
    <div style="margin-top: 12px; text-align: center;">
        <a href="{{mobileAppInfo.appStoreUrl}}" style="background: #f59e0b; color: white; padding: 8px 16px; text-decoration: none; border-radius: 20px; font-size: 12px; margin-right: 8px;">📱 iOS App</a>
        <a href="{{mobileAppInfo.googlePlayUrl}}" style="background: #f59e0b; color: white; padding: 8px 16px; text-decoration: none; border-radius: 20px; font-size: 12px;">🤖 Android App</a>
    </div>
</div>
{{/if}}

<div class="message">
    <p>Please review your updated targets in the dashboard or mobile app. If you have any questions about these changes, contact your manager or our support team.</p>
</div>

{{/base}} 