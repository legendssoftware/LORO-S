{{#base headerTitle="📊 Target Updated" headerSubtitle="Your target has been modified - {{userName}}"}}

<div class="greeting">
    Hello {{userName}}! 📊
</div>

<div class="message">
    <p>Your target at {{organizationName}} has been updated. The changes are now active and will be reflected in your performance tracking.</p>
</div>

<div class="info-notice">
    <p>📝 <strong>Target Updated!</strong> Your target configuration has been modified with the latest requirements.</p>
</div>

<div class="info-card">
    <h3>📊 Updated Target Details</h3>
    <div class="detail-grid">
        <div class="detail-item">
            <span class="detail-label">Employee</span>
            <span class="detail-value">{{userName}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Email</span>
            <span class="detail-value">{{userEmail}}</span>
        </div>
        {{#if targetDetails.dailyHours}}
        <div class="detail-item">
            <span class="detail-label">Daily Hours Target</span>
            <span class="detail-value">{{targetDetails.dailyHours}} hours</span>
        </div>
        {{/if}}
        {{#if targetDetails.monthlyHours}}
        <div class="detail-item">
            <span class="detail-label">Monthly Hours Target</span>
            <span class="detail-value">{{targetDetails.monthlyHours}} hours</span>
        </div>
        {{/if}}
        {{#if targetDetails.description}}
        <div class="detail-item">
            <span class="detail-label">Description</span>
            <span class="detail-value">{{targetDetails.description}}</span>
        </div>
        {{/if}}
        <div class="detail-item">
            <span class="detail-label">Last Updated</span>
            <span class="detail-value">{{formatDate updatedAt}}</span>
        </div>
    </div>
</div>

<div class="cta-section">
    <a href="{{dashboardUrl}}/targets" class="cta-button">View All Targets</a>
</div>

<div class="message">
    <p>Please review your updated targets in the dashboard. If you have any questions about these changes, contact your manager or our support team.</p>
</div>

{{/base}} 