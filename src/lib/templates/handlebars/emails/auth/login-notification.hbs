{{#base headerTitle="Login Security Alert" headerSubtitle="New sign-in detected"}}

<div class="greeting">
    Hello {{name}}! 🔐
</div>

<div class="message">
    <p>We detected a new sign-in to your account. If this was you, no action is needed. If you don't recognize this activity, please secure your account immediately.</p>
</div>

<div class="info-card">
    <h3>📍 Login Details</h3>
    <div class="detail-grid">
        <div class="detail-item">
            <span class="detail-label">Time</span>
            <span class="detail-value">{{loginTime}}</span>
        </div>
        {{#if location}}
        <div class="detail-item">
            <span class="detail-label">Location</span>
            <span class="detail-value">{{location}}{{#if country}}, {{country}}{{/if}}</span>
        </div>
        {{/if}}
        {{#if deviceType}}
        <div class="detail-item">
            <span class="detail-label">Device</span>
            <span class="detail-value">{{deviceType}}</span>
        </div>
        {{/if}}
        {{#if browser}}
        <div class="detail-item">
            <span class="detail-label">Browser</span>
            <span class="detail-value">{{browser}}</span>
        </div>
        {{/if}}
    </div>
</div>

{{#if suspicious}}
<div class="error-notice">
    <h4>🚨 Suspicious Activity Detected</h4>
    <p>This login appears unusual for your account. If this wasn't you, please secure your account immediately.</p>
</div>

<div class="cta-section">
    <a href="{{secureAccountUrl}}" class="cta-button">Secure My Account</a>
</div>
{{else}}
<div class="success-notice">
    <h4>✅ Login Successful</h4>
    <p>Your account is secure. Remember to always log out from shared devices.</p>
</div>
{{/if}}

<div class="message">
    <p><strong>Wasn't you?</strong> Please change your password immediately and contact our support team.</p>
</div>

{{/base}} 