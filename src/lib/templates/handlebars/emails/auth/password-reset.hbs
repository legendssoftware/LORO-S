{{#base headerTitle="Password Reset Request" headerSubtitle="Reset your account password securely"}}

<div class="greeting">
    Hello {{name}}!
</div>

<div class="message">
    <p>🔑 <strong>Password Reset Request</strong> - We received a request to reset your {{appName}} account password.</p>
    
    <p>Click the secure button below to reset your password. This link will expire in {{#if expiryHours}}{{expiryHours}} hours{{else}}24 hours{{/if}} for your security.</p>
</div>

<div class="cta-section">
    <a href="{{resetLink}}" class="cta-button">Reset Password Now</a>
    <a href="{{dashboardUrl}}" class="cta-button" style="background-color: #7c2d92; margin-left: 12px;">Go to Dashboard</a>
</div>

<div class="info-card">
    <h4>🔒 Reset Details</h4>
    <div class="detail-grid">
        <div class="detail-item">
            <span class="detail-label">Reset Link Status</span>
            <span class="detail-value">Active and ready to use</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Expires In</span>
            <span class="detail-value">{{#if expiryHours}}{{expiryHours}} hours{{else}}24 hours{{/if}}</span>
        </div>
        <div class="detail-item">
            <span class="detail-label">Security Level</span>
            <span class="detail-value">High - Single use only</span>
        </div>
    </div>
</div>

<div class="message">
    <p><strong>Having trouble with the button?</strong> Copy and paste this secure link into your browser:</p>
    <p style="word-break: break-all; color: #A855F7; font-family: monospace; background: #f8fafc; padding: 10px; border-radius: 6px; margin: 10px 0;">{{resetLink}}</p>
</div>

<div class="info-card">
    <h4>💡 Creating a Strong Password</h4>
    <ul class="feature-list">
        <li><strong>Length:</strong> Use at least 12 characters for maximum security</li>
        <li><strong>Complexity:</strong> Include uppercase, lowercase, numbers, and symbols</li>
        <li><strong>Uniqueness:</strong> Don't reuse passwords from other accounts</li>
        <li><strong>Memorability:</strong> Consider using a passphrase with random words</li>
    </ul>
</div>

<div class="warning-notice">
    <p>⚠️ <strong>Important:</strong> If you did NOT request this password reset, please ignore this email. Your password will remain unchanged, and this link will expire automatically.</p>
</div>

<div class="info-card">
    <h4>📧 What Happens Next?</h4>
    <p>Here's what you can expect:</p>
    <ol class="numbered-list">
        <li><strong>Secure Reset:</strong> Click the reset button above to securely reset your password</li>
        <li><strong>Password Creation:</strong> Create a strong, unique password when prompted</li>
        <li><strong>Confirmation:</strong> You'll receive a confirmation email after the password is changed</li>
        <li><strong>Auto Logout:</strong> All other devices will be logged out for security</li>
    </ol>
</div>

<div class="message">
    <p>This password reset link is valid for {{#if expiryHours}}{{expiryHours}} hours{{else}}24 hours{{/if}} and can only be used once. After clicking the link, you'll be guided through the secure password reset process.</p>
</div>

<div class="success-notice">
    <p>💚 <strong>Need Help?</strong> Our support team is available 24/7 to assist with account security questions at <a href="mailto:{{supportEmail}}" style="color: #15803d; font-weight: 600;">{{supportEmail}}</a></p>
</div>

{{/base}} 